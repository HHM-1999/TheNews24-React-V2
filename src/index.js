import React from 'react';
import ReactDOM from 'react-dom/client';
import Emyth from './Emyth';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
    <React.StrictMode>
      <Emyth />
    </React.StrictMode>
  </>
);
